services:
  app-core:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8080:8080"
      - "8081:8081"
    logging:
      driver: loki
      options:
        loki-url: "http://monitor.study-up.site:3100/loki/api/v1/push"
